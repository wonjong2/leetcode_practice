/**
 * @param {number} m
 * @param {number} n
 * @return {number}
 */
var uniquePaths = function (m, n) {
    let result = 0;

    goFinish(m, n);

    return result;

    function goFinish(low, column) {

    }
};